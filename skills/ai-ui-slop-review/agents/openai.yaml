interface:
  display_name: "AI UI Slop Review"
  short_description: "Review rendered AI UI before shipping"
  default_prompt: "Use $ai-ui-slop-review to audit this rendered interface and prioritize evidence-backed fixes."
